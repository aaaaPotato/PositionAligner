# 语音稿定位助手 · 设计说明

> 目标：通过麦克风收音，实时判断用户读到了稿子的哪个位置，在界面上高亮提示，不需要逐字对齐，只要“大概到哪一段”。要求足够轻量，低耗电、低发热。

---

## 1. 目标与范围

### 1.1 核心能力
- 导入稿子（TXT / PDF，二期支持 Word / 屏幕 OCR）。
- 通过麦克风连续收音，不联网，在本地完成识别。
- 识别用户的朗读，映射到稿子中的大致位置，并在 UI 上高亮当前所在段落。
- 支持**中英混合**稿子。

### 1.2 不做什么（第一版）
- 不做逐字校对、不做语法纠错、不做朗读评分。
- 不做云端 ASR（全部本地，省流量）。
- 不做屏幕文字 OCR、不做 Word 直读（二期再说）。
- 位置指示为“段落/大概位置”级别，不追求字级精度。

### 1.3 硬性指标（轻量）
- 静音/停顿时不做推理（VAD 门控）。
- 模型使用 int8 量化 + 16kHz，CPU 推理，不用 GPU。
- 体温手感：连续朗读 30 分钟，手机不烫手、不掉电异常快。

---

## 2. 总体架构

```
 ┌──────────────┐    ┌──────────┐   ┌─────────────┐   ┌──────────────┐
 │  麦克风采集    │───▶│  VAD 门控  │──▶│  本地识别引擎  │──▶│  文本位置匹配   │
 │  16kHz 单声道 │    │ 有人声才跑 │   │ sherpa-onnx │   │ 模糊匹配+滑窗 │
 └──────────────┘    └──────────┘   └─────────────┘   └──────┬───────┘
                                                              │
                          稿子导入 ────────────┐   读取进度      ▼
                          (TXT/PDF)           │            ┌──────────┐
                                               └───────────▶│  UI 高亮  │
                                                           └──────────┘
```

按模块划分：

| 模块 | 职责 |
|------|------|
| 采集模块 | 16kHz / 单声道 / 16bit 采样，切 20ms 帧 |
| VAD 门控 | 判断人声，静音丢帧，攒够人声片段才识别 |
| 识别引擎 | sherpa-onnx + SenseVoice，中英混读，输出文字 |
| 文本定位 | 模糊匹配识别文本 → 稿子位置，滑动窗口 |
| 稿子导入 | 解析 TXT / PDF 为纯文本 + 分段结构 |
| UI | 展示稿子、高亮当前段、显示进度 |

---

## 3. 核心模块设计

### 3.1 麦克风采集
- 采样率 **16000 Hz**（语音识别标准），单声道、16-bit PCM。
- 按 **20ms** 一帧（320 采样点/帧）处理。
- 平台：Python 用 `sounddevice` / `pyaudio`；Android 用 `AudioRecord`。

### 3.2 VAD 门控（省电关键）
- 两种实现：
  - **WebRTC VAD**：轻量、快速，适合当“粗过滤”。
  - **Silero VAD**（PyTorch，可转 ONNX）：更准，可做精细过滤。
- 逻辑：
  1. 每帧判 VAD，不是人声则丢弃，CPU 空转（处于低功耗读/休眠）。
  2. 连续检测到人声后开始组包，攒  1~2 秒的“语音片段”。
  3. 片段结束（检测到静音 > 阈值）或达到最大长度，交给识别引擎。
- **收益**：朗读停顿、翻页、思考时的静音帧完全不跑推理，是省电的最大头。

### 3.3 识别引擎
- 引擎：**sherpa-onnx**（k2-fsa 开源，Apache-2.0）。
  - 自带 Python API 和 Android AAR，同一模型两端通用。
- 模型：**SenseVoiceSmall**（阿里开源）int8 量化版本。
  - 支持中文 + 英文 + 中英混读（开箱即用）。
  - 体积小、CPU 推理快，比 whisper 省电。
- 配置：
  - 输入 16kHz 单声道。
  - 识别每 1~2 秒片段，输出一句文字。
  - 关闭不需要的附加输出（情绪/事件标签），减少计算。

### 3.4 文本位置匹配（心算法，不需要逐字）
目标：给定识别文本 `hyp`，在稿子 `doc` 中找到它对应的大致位置。

做法：
1. **分词/分字**：中文按字或分词；英文按空格分 token。统一切成 `token[]`。
2. **滑动窗口跟踪**：记录“上次位置 `lastPos`”，只在 `lastPos` 附近（如前后 N 个 token）窗口内搜索，避免跳段误判。
3. **子序列/重叠匹配**：计算 `hyp` 与窗口内候选段的重叠 token 数，选重合度最高者作为当前位置。
4. **容错**：允许错字、漏字、同音字。重合 token 比例达到阈值即命中。
5. **失败兜底**：连续多次匹配不上时，提示“跟上进度困难，请大声清晰读一句”，不打断朗读，位置保持上次。

匹配示例：
> 识别：`我们已经完成任务 下一部分是总结`
> 稿子：`……我们已经部署完成，接下来… 第二部分 总结……`

因滑动窗口 + 词重叠，即便“部署完成”被识别成“完成任务”，仍能靠“总结”定位到对应段落。

### 3.5 稿子导入
- TXT：直接读 UTF-8 文本，按段落/自然段切分。
- PDF（一期）：Android 用系统 `PdfRenderer` 提取文本；Python 用 `pypdf`/`pdfplumber`。
- Word（二期）：Apache POI 或建议先另存为 PDF/TXT。
- 屏幕 OCR（二期）：MediaProjection + 前台服务 + ML Kit，重，不建议第一版。

### 3.6 定位进度回读
- 保存 `lastPos` 到本地（进程被杀/关闭后，下次能接着从上次位置开始）。
- UI 上高亮当前段 + 显示“本稿进度百分比 / 本段还剩多少”。

---

## 4. 省电与轻量策略（汇总）

| 手段 | 效果 |
|------|------|
| VAD 门控：静音不推理 | 最大头，静默时 CPU 空转 |
| int8 量化模型 + 16kHz | 计算量小、体积小 |
| 分段推理（1~2s）而非全篇转码 | 减少无效计算 |
| 引用缓存：同一段不重复匹配 | 减少重复计算 |
| 不上 GPU/唤醒词，普通 CPU 推理 | 降低功耗 |

---

## 5. 落地路线（分阶段）

| 阶段 | 内容 | 技术栈 | 目的 |
|------|------|--------|------|
| **①** | Python 桌面原型：Mic→VAD→识别→位置匹配→PySide6 高亮 | Python + sherpa-onnx + PySide6 + sounddevice | 端到端验证核心算法是否可行 |
| **②** | 用真实稿子调参数（滑动窗口大小、容错阈值） | ① | 让定位准确、稳定 |
| **③** | 逻辑验证 OK 后移植 Android MVP | Kotlin + sherpa-onnx AAR | 手机端真正能用 |
| **④** | 二期：Word 直读、屏幕 OCR、进度持久化增强等 | 按需 | 功能扩展 |

> 说明：阶段① 的算法与模型和阶段③ 完全一致，只是界面层不同。先做① 能把最大的风险（识别+定位是否靠谱）在开发者熟悉的环境中解决掉。

---

## 6. 关键依赖清单

- **Python 原型**
  - `sherpa-onnx`（识别引擎，含 SenseVoice 模型）
  - `sounddevice` / `pyaudio`（麦克风）
  - `onnxruntime`（VAD 模型推理，如需 Silero）
  - `PySide6`（UI）
  - `pypdf` / `pdfplumber`（PDF 解析）
- **Android 版**
  - sherpa-onnx Android AAR
  - Kotlin + Jetpack Compose / XML
  - `PdfRenderer`（系统 PDF 解析）

---

## 7. 开放问题 / 待确认
- 稿子典型长度？（决定滑动窗口大小与索引方式）
- 定位精度需要到“段”还是“句”？（影响容错阈值）
- 是否需要“读到指定位置时发出提醒”（可做，但会增加逻辑）
- 是否介意先把 Python 原型跑起来感受下效果再决定是否做手机版。